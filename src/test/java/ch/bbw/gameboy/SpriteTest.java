package ch.bbw.gameboy;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

public class SpriteTest {

    @Test
    void spriteTest() {
        //assertDoesNotThrow(() -> new Sprite("sprites\\enemy1", null));

        assertDoesNotThrow(() -> {});
        assertTrue(true);

    }
}
